//
//  AppFeatureFlags.swift
//  FrameFlow
//

import Foundation

enum AppFeatureFlags {
    /// Captions UI + transcription + burn-in disabled for launch. Re-enable when stable.
    static let captionsEnabled = false
}
